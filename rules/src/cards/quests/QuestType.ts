export enum QuestType {
  Clue, Chimera, Rock, Thistle, Color, AllColor, Night, BrutPoints, WonderSet,
  // Starry Skies extension
  RequireColor, RequireClue, RequireNight, MultiWonderSum, Sacrifice
}